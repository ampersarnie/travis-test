language: php
cache:
  directories:
  - "$HOME/.composer/cache"
  - "$HOME/.yarn"
  - "./node_modules"
before_install:
- yarn
jobs:
  include:
  - stage: master-build
    script:
    - "./bin/push.sh"
    if: branch = master

